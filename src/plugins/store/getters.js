const getters = {
  rootApp: state => state.app.rootApp,
  settings: state => state.app.settings,
  themeManager: state => state.app.themeManager,
  showSide: state => state.app.showSide,
}

export default getters